КРИТИЧЕСКИЕ (CRITICAL)
1. Bypass авторизации администратора
WebApp/Areas/AuthController.cs:45-50 — Любой пользователь может войти как админ, просто отправив "Admin" в email. Нет проверки окружения (dev/prod), нет ограничений.
2. Удаление всей БД без авторизации
WebApp/Areas/Api/DatabaseController.cs:12-28 — Эндпоинт DELETE /api/database не требует авторизации. В dev-окружении любой запрос стирает всё.
3. Подмена ответов (Answer Impersonation)
WebApp/Areas/Api/AnswerController.cs:18-55 — При создании ответа UserId берётся из тела запроса, а не из сессии. Любой авторизованный пользователь может отправлять ответы от чужого имени. Также нет проверки, что пара (reviewer, target) вообще существует в матрице назначений.
ВЫСОКИЕ (HIGH)
4. NullReferenceException в Login
AuthController.cs:23 — request.Email.Trim() вызывается до проверки на null. Если Email == null — crash.
5. NullReferenceException в проверках статуса
SurveyService.cs:17, 23 — status.Trim() без null-проверки. Если в БД окажется null статус — crash во всех контроллерах.
6. Нет авторизации на User CRUD
UserController.cs — Создание, получение, список пользователей — всё доступно без авторизации.
7. Нет авторизации на Survey Template CRUD
SurveyTemplateController.cs — Все операции с шаблонами доступны без авторизации, включая создание реальных опросов из шаблона.
8. Нет авторизации на Settings
SettingsController.cs:12 — Конфигурация приложения доступна всем.
9. Нет авторизации на чтение вопросов
QuestionController.cs:57 — Любой может читать вопросы и все ответы на них.
10. credentials: 'include' отсутствует на скачивании отчётов
frontend/src/api.ts:153, 177 — downloadReport и downloadCsv используют fetch() без credentials: 'include'. Если эндпоинты защищены — скачивание молча не работает.
11. Object URL отозван до завершения скачивания
frontend/src/api.ts:168-173, 192-197 — URL.revokeObjectURL(url) вызывается сразу после link.click(), что может убить скачивание до начала.
СРЕДНИЕ (MEDIUM)
12. Race condition: автозакрытие опроса
SurveyService.cs:344-398 — Два конкурентных запроса CompleteAssignment могут оба прочитать allAssignedCompleted = false перед сохранением, и опрос никогда не закроется автоматически.
13. Race condition: upsert ответа
AnswerController.cs:35-55 — Проверка-then-действие не атомарны. Два конкурентных запроса на одинаковый (QuestionId, UserId, TargetId) вызовут дублирование или ошибку БД.
14. Null-проверка connection string
Program.cs:14 — GetConnectionString() может вернуть null. UseSqlite(null) упадёт с непонятной ошибкой.
15. Вопросы можно менять/удалять на активных опросах
QuestionController.cs:76-118 — Нет проверки статуса опроса при обновлении/удалении вопросов.
16. Назначения можно перезаписывать на активных опросах
SurveyService.cs:255 — SaveAssignmentsAsync не проверяет статус опроса.
17. Неверный HTTP-статус: Forbid вместо NotFound
QuestionController.cs:84 — Если survey == null, возвращается 403 вместо 404.
18. CompleteAssignment не проверяет статус опроса
SurveyService.cs:344 — Можно завершать назначения на черновике или уже закрытом опросе.
19. CreateSurveyFromTemplate не ставит CreatedByUserId
SurveyTemplateController.cs:156-164 — Созданный из шаблона опрос невидим для создателя (фильтр по CreatedByUserId).
20. Нет credentials в download-запросах (фронтенд)
api.ts:153, 177 — Скачивание отчётов не отправляет cookie.
21. EntityPage получает id={0}
App.tsx:81 — При selectedSurveyId == null компонент получает невалидный id=0.
22. JSON.parse без try-catch
mappers.ts:120, TemplatesModal.tsx:192, TemplateEditor.tsx:275 — Невалидный JSON в props крашит приложение.
23. Expanded MatrixTable не получает exportingCsv и canExport
MainPage.tsx:289-311 — В модальном режиме матрицы нет кнопки CSV-экспорта.
НИЗКИЕ (LOW) / CODE SMELLS
24. CORS захардкожен на localhost
Program.cs:62 — Не будет работать в продакшене.
25. Swagger-сервисы??ованы в проде
Program.cs:25-26 — AddEndpointsApiExplorer и AddSwaggerGen включены всегда.
26. Все опросы грузятся в память
SurveyService.cs:63 — ToListAsync() без Where — фильтрация в C# вместо SQL.
27. O(n?) генерация CSV
SurveyCsvReportService.cs:83 — Линейный перебор ответов на каждый вопрос.
28. SetNull на non-nullable FK
ApplicationDbContext.cs:42-50 — Потенциальная ошибка при удалении пользователя.
29. SignalR??ует всем клиентам
SurveyController.cs:22 — Уведомления приходят даже тем, кто смотрит другой опрос.
30. Неиспользуемый пакет Npgsql
WebApp.csproj:17 — PostgreSQL-провайдер подключён, но используется SQLite.
31. Избыточное каскадное удаление ответов
SurveyService.cs:161 — Ручное удаление ответов перед удалением вопросов (уже есть каскад).
32. alert() вместо нормального UI
TemplatesModal.tsx:62, 199 — Блокирующие alert вместо модалок.
33. Двойной вызов parseSurveyResponseParams()
useSurveyTaking.ts:22, 31 — Лишний вызов при монтировании.
34. IIFEs в JSX
MatrixTable.tsx:352-372 — Новые функции каждый рендер, ломают мемоизацию.